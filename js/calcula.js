function getMensagemMenu(){
    var op = ""
    op = prompt("Entre com a operação: \n(+) soma \n(-) subtrai \n(*)mutiplica \n(/) divide \n(%) resto da divisão")
    return op
}
function calcula(operador, operando1, operando2){
    var tot = 0
    switch (op){
        case "+":

            break
        case "-":

            break
        case "*":

            break
    }
}